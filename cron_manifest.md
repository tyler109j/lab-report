# Cron Job Manifest

_Auto-generated by `cron_manifest.py` at 2026-06-21T15:10:35Z._  
_Source of truth: `hermes cron list` + script header comments._

**Total active jobs:** 15

Naming convention: `lab-<category>-<verb>`. Categories:

- `lab-research-*` — backtest cycles + validation
- `lab-data-*` — OANDA candle pulls, OOS tests
- `lab-report-*` — metrics, dashboard render, publish
- `lab-safety-*` — blocker fix, load monitor, kill-switch checks
- `lab-paper-*` — forward-test observation (not yet eligible)

---

## 🔬 Research (cycles, backtests, validation)

| Name | Schedule | Deliver | What it does |
|---|---|---|---|
| `lab-research-cycle` | `*/15 * * * *` | local | _(LLM-driven prompt; see `hermes cron list` for the body)_ |
| `lab-research-meta-cycle` | `0 2 * * *` | local | _(LLM-driven prompt; see `hermes cron list` for the body)_ |
| `lab-research-weekly-revalidate` | `0 6 * * 0` | local | weekly_revalidate.sh - full gauntlet re-validation. Runs every Sunday 06:00 UTC. |
| `lab-research-fast-cycle` | `2,7,12,17,22,27,32,37,42,47,52,57 * * * *` | local | Thin wrapper for Hermes cron — delegates to the canonical script in /root/lab/ so all the path-relative logic (cd "$(dirname "$0")", .lab-py.sh, logs/, etc.) keeps working as designed. |

## 📥 Data (OANDA feed, OOS pulls)

| Name | Schedule | Deliver | What it does |
|---|---|---|---|
| `lab-data-daily-refresh` | `0 5 * * *` | local | daily_refresh_data.sh - refresh OANDA candle cache daily. Keeps the cached data within 1 day of "now" so the backtest engine has the latest bars for forward tests, walk-forward, and signal generation. Idempotent: data.py skips if already cached. |
| `lab-data-forward-test` | `18 * * * *` | local | _(LLM-driven prompt; see `hermes cron list` for the body)_ |
| `lab-data-rolling-forward` | `8 */6 * * *` | origin | _(LLM-driven prompt; see `hermes cron list` for the body)_ |

## 📊 Report (metrics, render, publish)

| Name | Schedule | Deliver | What it does |
|---|---|---|---|
| `lab-report-hourly-summary` | `0 * * * *` | origin | _(LLM-driven prompt; see `hermes cron list` for the body)_ |
| `lab-report-weekly-audit` | `30 5 * * 0` | origin | weekly_audit.sh — generate the live-readiness report.  1. Extract every metric into dashboard/metrics.json (single source of truth) 2. Render dashboard/index.html from the snapshot + live equity series 3. Push to the public GitHub Pages mirror (if anything changed)  Scheduled Sundays 05:30 UTC by the lab-weekly-audit cron — runs BEFORE weekly_revalidate so the dashboard reflects the latest gauntlet. |
| `lab-report-equity-refresh` | `4 * * * *` | local | Thin wrapper for Hermes cron — delegates to /root/lab/lab-equity-refresh.sh |
| `lab-report-goal-check` | `5 11 * * *` | origin | Thin wrapper for Hermes cron — delegates to /root/lab/lab-goal-check.sh. |

## 📄 Paper (forward-test observation)

| Name | Schedule | Deliver | What it does |
|---|---|---|---|
| `lab-paper-track` | `13,28,43,58 * * * *` | local | paper_track.sh - paper-trade signal logger. Runs every 15 min via cron. Loads OANDA key from the canonical env file (only OANDA_API_KEY needed for the live runner since account_id is auto-discovered). |

## 🛡️ Safety (blockers, load monitor, kill switch)

| Name | Schedule | Deliver | What it does |
|---|---|---|---|
| `lab-safety-stale-scrubber` | `0 3 * * 0` | local | Thin wrapper for Hermes cron — delegates to /root/lab/lab-stale-run-scrubber.sh |
| `lab-safety-blocker-loop` | `0 4 * * *` | origin | Thin wrapper for Hermes cron — delegates to /root/lab/lab-blocker-loop.sh |
| `lab-safety-load-monitor` | `17 * * * *` | origin | Thin wrapper for Hermes cron — delegates to the canonical script in /root/lab/ so all the path-relative logic (cd "$(dirname "$0")", .lab-py.sh, logs/, etc.) keeps working as designed. |

---

_Last refreshed: see the `_Auto-generated_` timestamp above._  
_Re-run anytime with `python3 cron_manifest.py`._
